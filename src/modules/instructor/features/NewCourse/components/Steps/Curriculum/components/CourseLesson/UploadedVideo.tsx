import React, { useRef, useState } from 'react';

interface ThumbnailGeneratorProps {
  videoFile: File;
}

function ThumbnailGenerator({ videoFile }: ThumbnailGeneratorProps) {
 
    
  return (
    <div>
    
    </div>
  );
}

export default ThumbnailGenerator;
